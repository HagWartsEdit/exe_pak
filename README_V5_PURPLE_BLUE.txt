EXE PAK ULTIMATE V5 — PURPLE / BLUE EDITION

این نسخه بر پایه EXE_PAK_ULTIMATE_V4_COMPLETE ساخته شده است.

تغییرات اصلی:
- تم سراسری بنفش + آبی با glow و گرادیان‌های جدید.
- اصلاح جریان ثبت بازدید صفحه اصلی: دیگر load به صورت بازگشتی بعد از pageview اجرا نمی‌شود.
- pageview با cache: no-store و sessionStorage کنترل می‌شود.
- /api/stats مقاوم‌تر شد و site_stats را قبل از خواندن تضمین می‌کند.
- /api/stats/ping برای تشخیص سریع وضعیت ردیف site_stats اضافه شد.
- قابلیت‌های نسخه V4 حفظ شده‌اند.

تست:
- ZIP integrity check
- JavaScript syntax check برای src/index.js
