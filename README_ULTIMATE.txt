EXE PAK ULTIMATE COMMUNITY UPDATE

Email has been removed from the UI and API for this version. Existing D1 data is preserved.
New migrations are automatic: featured, staff_pick, tags, comment replies, favorites, ratings, notifications.
New pages: creator.html.
Do NOT delete the existing D1 database.

wrangler.toml keeps the existing D1 database_id.
